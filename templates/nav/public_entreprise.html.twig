<div class="navbar navbar-expand-md navbar-light bg-light">
    <a class="navbar-brand titre1 mr-auto" id="brand" href="{{ path("home") }}">CANDIDATHE<i class="fa fa-search" aria-hidden="true"></i>UE</a>
    {% if is_granted('IS_ANONYMOUS') %}
        <a class="navbar-brand titre1 text-right" id="brand" href="{{ path("espace_entreprise") }}"><h4>ESPACE ENTREPRISE</h4></a>
        <button class="btn btn-info btn-round" data-toggle="modal" data-target="#loginModal">Connexion</button>&nbsp &nbsp
    {% endif %}
</div>

